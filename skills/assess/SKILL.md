---
description: Assess a task's complexity, ambiguity, novelty, and verification strength into a validated rigor.routing-input.v2 with repository-anchored evidence and explicit confidence. Use before `rigor route` whenever no human-authored routing input exists, including inside an autonomous `/rigor:orchestrate` flow.
argument-hint: <task-statement-or-issue-ref> <preflight-json> <model-profiles-json> [availability-json]
allowed-tools: Read, Grep, Glob, Write, Bash(rigor route *), Bash(rigor availability *)
---

Read the supplied task statement or issue reference and only the repository files it names, the tests it touches, and directly related schemas — a handful of targeted reads, never a bulk scan of the repository, and never transmitted anywhere outside this session. Judge `complexity`, `ambiguity`, and `novelty` (`low`/`medium`/`high`/`critical`) and `verificationStrength` (`weak`/`moderate`/`strong`) only from what you actually read.

Write a `rigor.routing-input.v2` JSON file outside the repository (a temp or session directory, never inside the working tree) with `signals`, `budget`, and an `assessment` object carrying an explicit `confidence` (`low`/`medium`/`high`) and 1 to 20 `evidence` entries. Each evidence entry anchors one `observation` to a repository-relative `path` (no absolute paths, no `..` segments). Every signal judgment must trace to at least one evidence entry — never assert a signal with no supporting observation. Evidence observations state facts you found (for example "src/x.ts hard-codes a single retry with no backoff"), never a model name, capability tier, or a conclusion such as "use model Z": this skill never names or selects a model, and candidate selection remains exclusively a `rigor route` CLI decision made after validation.

Before any `--record`, validate the written input with `rigor route --dry-run --preflight <preflight-json> --input <the-written-assessment> --profiles <model-profiles-json>` (add `--availability <availability-json>` when supplied). Exit `0` with `status: "selected"` means the input validated and an eligible candidate exists. Exit `3` means the input itself is malformed, unsupported, evidence-free, or internally contradictory (for example high confidence together with critical ambiguity or weak verification) — fix it only if you find an actual factual error in your own signals or evidence, never by loosening them to force a pass. Exit `2` with `status: "requires-review"` or `"unroutable"` means stop: hand the assessment and its evidence to a human instead of retrying with adjusted signals or raised confidence.

Stop and do not produce a routing input at all when: the available evidence is insufficient or contradictory and cannot support at least a reviewable judgment; the task statement lacks acceptance criteria a contract could use; or the task names a decision only a human can make (an irreversible action, a policy exception, an unresolved scope owner). State plainly that this assessment is reviewable judgment, not a deterministic fact, and that configured runtime identity stays unverified regardless of the outcome.
